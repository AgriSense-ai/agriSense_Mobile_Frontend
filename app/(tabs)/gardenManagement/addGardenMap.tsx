import { StyleSheet, Text, View } from "react-native";
import AddMap from "@/components/gardenManagement/AddMap";

const AddGardenMap = () => {
  return <AddMap />;
};

export default AddGardenMap;

const styles = StyleSheet.create({});
